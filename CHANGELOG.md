# Changelog

## [Unreleased] - 2025-12-22

### ✨ Добавлено

#### 1. Retry для Claude API
- Добавлена библиотека `tenacity` для автоматических повторных попыток при ошибках API
- Retry срабатывает при:
  - `RateLimitError` (429) — превышен лимит запросов
  - `APIStatusError` (500+) — ошибки сервера
- Стратегия: exponential backoff (2s, 4s, 8s, ..., до 30s)
- Максимум 3 попытки
- **Файлы**: `src/services/llm.py`, `pyproject.toml`

#### 2. Кэширование системного промпта Claude
- Реализовано prompt caching через `cache_control` API
- Экономия до 90% токенов на системном промпте
- Кэш живёт 5 минут
- Логирование использования кэша (`cache_read_input_tokens`)
- **Файлы**: `src/services/llm.py`

#### 3. Парсинг произвольного времени встречи
- Добавлен state `MEETING_CUSTOM_TIME` для ожидания ввода времени
- Функция `parse_custom_meeting_time()` использует Claude для парсинга естественного языка
- Поддерживаемые форматы:
  - "завтра в 15:00"
  - "в среду в 11:00"
  - "28 декабря, 14:00"
- Валидация времени (должно быть в будущем)
- **Файлы**: `src/services/llm.py`, `src/handlers/meetings.py`, `src/handlers/states.py`

#### 4. Follow-up напоминания
- Добавлено поле `follow_up_count` в модель `Lead`
- Создан планировщик `src/services/scheduler.py`:
  - Проверка каждый час
  - 1-е напоминание через 24 часа без ответа
  - 2-е напоминание через 48 часов
  - После 2-х напоминаний → статус COLD
- Автоматический сброс счётчика при ответе лида
- Интеграция в `bot.py` (запуск в фоне)
- **Файлы**: `src/services/scheduler.py`, `src/database/models.py`, `src/bot.py`, `src/handlers/conversation.py`, `src/handlers/start.py`

### 🔧 Изменено

- Обновлены все обработчики для отслеживания `last_message_at` с timezone-aware datetime
- Заменён `datetime.utcnow()` на `datetime.now(tz=timezone.utc)` во всех файлах
- Добавлена функция `_update_last_message_time()` для централизованного обновления времени

### 📦 Зависимости

- Добавлена `tenacity>=8.2.0`

### 🗄️ Миграции

- Создана миграция `1_20251222222744_add_follow_up_count.py` для нового поля `follow_up_count`

---

## Критерии успеха (выполнены ✅)

### Задача 1: Retry для Claude API
- ✅ При ошибке 429 бот ждёт и повторяет запрос (до 3 раз)
- ✅ В логах видно "Retry 1 after error..." (через `before_sleep_log`)

### Задача 2: Кэширование промпта
- ✅ При повторных запросах в логах видно "cache hit (N cached tokens)"
- ✅ Снижение стоимости API (до 90% на системном промпте)

### Задача 3: Парсинг времени
- ✅ "завтра в 15:00" → корректная дата
- ✅ "в пятницу в 10:00" → корректная дата
- ✅ Невалидный ввод → "Не смог понять время 😕"

### Задача 4: Follow-up
- ✅ Через 24 часа без ответа — лид получает напоминание
- ✅ Через 48 часов — второе напоминание
- ✅ После 2-х напоминаний — статус COLD

---

**Дата реализации**: 22 декабря 2025
**План**: `plans/005-high-priority-improvements.md`
