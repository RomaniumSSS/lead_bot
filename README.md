# 🤖 AI Sales Assistant для малого бизнеса

Автономный Telegram-бот, который работает 24/7, квалифицирует лиды, ведёт диалоги с потенциальными клиентами, назначает встречи и передаёт готовых клиентов владельцу.

---

## 📋 Возможности

- ✅ **Автоматическая квалификация лидов** — бот задаёт нужные вопросы (задача, бюджет, срок) и оценивает готовность клиента.
- ✅ **Умные диалоги через Claude AI** — естественное общение, персонализированные ответы.
- ✅ **Уведомления владельцу** — мгновенные уведомления о горячих и тёплых лидах.
- ✅ **Статистика в реальном времени** — команда `/stats` для владельца.
- ✅ **База данных PostgreSQL** — полная история диалогов и данные лидов.
- ✅ **Docker-ready** — простой деплой на VPS через Docker Compose.

---

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- PostgreSQL 15+ (или Docker)
- Telegram Bot Token (от [@BotFather](https://t.me/BotFather))
- Anthropic API Key (от [console.anthropic.com](https://console.anthropic.com/))

---

### 1. Установка зависимостей

#### Через uv (рекомендуется)

```bash
# Установить uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установить зависимости
uv sync
```

#### Через pip (альтернатива)

```bash
pip install -r pyproject.toml
```

---

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните:

```env
# Telegram Bot Token (получить: https://t.me/BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Anthropic Claude API Key (получить: https://console.anthropic.com/)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxx

# PostgreSQL Database URL
DATABASE_URL=postgres://salesbot:salesbot_password@localhost:5432/sales_assistant

# Ваш Telegram ID (найти: https://t.me/userinfobot)
OWNER_TELEGRAM_ID=123456789

# Информация о бизнесе
BUSINESS_NAME=Моя Веб-студия
BUSINESS_DESCRIPTION=Разработка сайтов и веб-приложений
```

---

### 3. Запуск БД (через Docker)

```bash
docker-compose up -d db
```

Или запустите PostgreSQL локально.

---

### 4. Применение миграций

```bash
# Инициализация Aerich (только первый раз)
uv run aerich init -t src.database.config.TORTOISE_ORM
uv run aerich init-db

# Или, если миграции уже есть:
uv run aerich upgrade
```

---

### 5. Запуск бота

```bash
uv run python -m src.bot
```

Бот запущен! 🎉

---

## 🐳 Запуск через Docker Compose (рекомендуется для продакшена)

### 1. Создайте `.env` файл

```bash
cp .env.example .env
# Заполните .env
```

### 2. Запустите всё одной командой

```bash
docker-compose up -d
```

### 3. Примените миграции

```bash
docker-compose exec bot uv run aerich upgrade
```

### 4. Проверьте логи

```bash
docker-compose logs -f bot
```

---

## 📖 Документация

Вся документация находится в папке `docs/`:

- **[docs/product.md](docs/product.md)** — описание продукта, User Flow, бизнес-логика.
- **[docs/tech.md](docs/tech.md)** — техническая архитектура, стек, структура кода.
- **[AGENTS.md](AGENTS.md)** — инструкции для AI-агентов (если работаете с LLM-помощниками).

---

## 🛠️ Структура проекта

```
ai-sales-assistant/
├── docs/                   # Документация
├── plans/                  # Планы задач
├── migrations/             # Миграции БД (Aerich)
├── src/                    # Исходный код
│   ├── database/           # Модели Tortoise ORM
│   ├── handlers/           # Telegram handlers
│   ├── services/           # Бизнес-логика (LLM, уведомления)
│   ├── middlewares/        # Aiogram middlewares
│   ├── utils/              # Утилиты (логгер)
│   ├── bot.py              # Главный файл запуска
│   └── config.py           # Загрузка .env
├── .env.example            # Пример переменных окружения
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile              # Docker образ
├── pyproject.toml          # Зависимости
└── README.md               # Этот файл
```

---

## 🔧 Команды для владельца

### `/start`
Приветствие и начало диалога (для лидов).

### `/help`
Справка о возможностях бота (для лидов).

### `/stats`
Статистика (только для владельца, проверяется `OWNER_TELEGRAM_ID`):
- Всего лидов
- Новых за сегодня
- По статусам: горячие 🔥, тёплые 🟡, холодные ❄️
- Назначенные встречи

---

## 📊 Статусы лидов

- **🔥 HOT (Горячий)** — чёткая задача + подходящий бюджет + срочно (на этой неделе).
- **🟡 WARM (Тёплый)** — задача понятна + средний бюджет + срок "скоро".
- **❄️ COLD (Холодный)** — неясная задача или низкий бюджет или "пока думаю".
- **⚪️ NEW (Новый)** — недостаточно информации для квалификации.

---

## 🧪 Разработка

### Проверка типов и качества кода

Проект использует **MyPy** для строгой типизации и **Ruff** для линтинга:

```bash
# Проверка типов
uv run mypy src/

# Линтинг и проверка стиля
uv run ruff check src/

# Автоматическое форматирование
uv run ruff format src/
```

### Создание новой миграции

После изменения моделей в `src/database/models.py`:

```bash
uv run aerich migrate --name "описание_изменения"
uv run aerich upgrade
```

### Тестирование (на будущее)

```bash
uv run pytest
```

---

## 🚨 Troubleshooting

### Ошибка: `ModuleNotFoundError: No module named 'src'`

Убедитесь, что запускаете через `python -m src.bot`, а не `python src/bot.py`.

### Ошибка подключения к БД

Проверьте:
1. PostgreSQL запущен (`docker-compose ps` или `systemctl status postgresql`).
2. `DATABASE_URL` в `.env` правильный.
3. База данных `sales_assistant` создана.

### Бот не отвечает

Проверьте:
1. Токен бота правильный (`TELEGRAM_BOT_TOKEN` в `.env`).
2. Логи: `docker-compose logs -f bot` или просмотрите `logs/bot.log`.
3. Anthropic API ключ правильный (`ANTHROPIC_API_KEY`).

---

## 📝 Roadmap (следующие версии)

- [ ] Автоматический follow-up (напоминания лидам).
- [ ] Nurture-цепочка для холодных лидов.
- [ ] Интеграция с Google Calendar (автоматическое добавление встреч).
- [ ] Веб-дашборд для владельца (FastAPI + React).
- [ ] Мультитенантность (поддержка нескольких бизнесов).
- [ ] Webhook вместо polling (для продакшена).
- [ ] Поддержка голосовых сообщений (Speech-to-Text).

---

## 🤝 Вклад

Проект в активной разработке. Если у вас есть идеи или вы нашли баг — создайте issue или pull request!

---

## 📄 Лицензия

MIT License

---

## 📧 Контакты

Если нужна помощь в настройке или кастомизации — напишите!

---

**Удачи в продажах! 🚀**

