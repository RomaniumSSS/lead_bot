# ⚡ Быстрый старт для разработчика

Шпаргалка с основными командами для работы над проектом.

---

## 🚀 Первый запуск

```bash
# 1. Настроить окружение
make setup

# 2. Создать .env
cp .env.example .env
# Отредактировать .env (токены, ключи)

# 3. Запустить БД
docker-compose up -d db

# 4. Применить миграции
make migrate

# 5. Запустить бота
make run
```

---

## 💻 Ежедневные команды

### Запуск бота
```bash
make run                    # Локально
make docker-up              # В Docker
make docker-logs            # Логи Docker
```

### Проверка кода
```bash
make check                  # Полная проверка (lint + types)
make format                 # Форматирование
make test                   # Тесты
```

### База данных
```bash
make migrate-create name="описание"    # Создать миграцию
make migrate                           # Применить миграции
```

### Docker
```bash
make docker-up              # Запустить
make docker-down            # Остановить
make docker-logs            # Логи
make docker-shell           # Войти в shell
```

---

## 📝 Workflow

```bash
# 1. Новая ветка
git checkout -b feature/новая-фича

# 2. Писать код...

# 3. Проверить
make check && make test

# 4. Коммит (pre-commit автоматически проверит)
git commit -m "feat: описание"

# 5. Push
git push origin feature/новая-фича
```

---

## 🔍 Полезные команды

```bash
make help               # Все команды
make status             # Статус окружения
make clean              # Очистить временные файлы

docker-compose ps       # Статус контейнеров (с healthcheck)
tail -f logs/bot.log    # Логи бота
```

---

## 📚 Документация

- **README.md** — установка и запуск
- **docs/development.md** — подробная инструкция по разработке
- **docs/tech.md** — техническая документация
- **docs/product.md** — описание продукта
- **AGENTS.md** — инструкции для AI-агентов

---

## 🆘 Проблемы?

```bash
# Переустановить зависимости
make clean
make install-dev

# Пересоздать БД (ОСТОРОЖНО!)
docker-compose down -v
docker-compose up -d db
make migrate

# Проверить окружение
make status
```

---

**Хорошей разработки! 🚀**
