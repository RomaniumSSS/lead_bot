# Инструкции для AI Агентов

Этот файл содержит критически важные инструкции для любого AI-агента или LLM, работающего с этим репозиторием.

## 1. Протокол Инициализации (ОБЯЗАТЕЛЬНО)

Прежде чем приступать к выполнению любой задачи, вы **должны**:

1. Прочитать `README.md` в корне проекта.
2. Прочитать **ВСЕ** файлы в директории `docs/`.

**Почему это важно?** Вы не можете эффективно работать над проектом, не понимая его архитектуру, бизнес-логику и текущий статус, описанные в документации. Не галлюцинируйте о том, как работает код — изучите документацию.

## 2. Политика Ведения Документации

Если вы вносите **ЗНАЧИМЫЕ** изменения в проект, вы **ОБЯЗАНЫ** обновить документацию.

### Что считается значимым изменением:

- Изменение глобального User Flow (пользовательского пути).
- Изменение архитектуры приложения.
- Добавление новых ключевых модулей или сервисов.
- Изменение контрактов API или структур данных, описанных в документах.
- Логические изменения, влияющие на бизнес-правила.

### Что НЕ требует обязательного обновления `docs/` (если не затрагивает суть):

- Простой рефакторинг кода без изменения логики.
- Добавление рядовых библиотек/зависимостей.
- Исправление мелких багов.

### Ваши действия:

- После внесения изменений проверьте, соответствуют ли файлы в `docs/` новой реальности.
- Если нет — отредактируйте их в том же Pull Request / коммите.

## 3. Базовые принципы (Core Principles)

- **Documentation-First Development** — каждая сессия начинается с чтения всех файлов в папке `docs/` (`product.md`, `tech.md`). Не начинай писать код, пока не поймешь текущую архитектуру и задачу.
- **Instruction-First** — Для простых задач следуй инструкциям человека напрямую. Для сложных задач — сначала планируй.
- **Keep It Simple (MVP)** — Мы делаем MVP для малого бизнеса. Избегай оверинжиниринга. Используй простые и понятные решения, соответствующие стеку (aiogram, Tortoise ORM, PostgreSQL, Anthropic Claude).

## 4. Режим планирования (Plan Mode)

### Когда использовать:

Задача считается **сложной** (и требует плана), если затрагивает:

- Множество модулей (например, `handlers` + `database` + `services`).
- Новые интеграции (внешние API, LLM).
- Изменение схемы БД (миграции).
- Архитектурные изменения.

**Если есть сомнения — считай задачу сложной.**

### Процесс:

Для сложных задач используется папка `plans/`.

1. Создай файл плана с именем в формате: `plans/NNN-objective.md` (где `NNN` — инкрементальный номер, например `001-initial-setup.md`).
2. Файл должен содержать:
   - **Objective (Цель)**: Что нужно сделать (дословно из запроса).
   - **Context (Контекст)**: Краткий контекст задачи.
   - **Proposed Steps (Предлагаемые шаги)**: Нумерованный список конкретных действий.
   - **Risks (Риски)**: Возможные проблемы.
   - **Rollback Strategy (Стратегия отката)**: Что делать, если все пойдет не так.
   - **Success Criteria (Критерии успеха)**: Как понять, что задача выполнена.

**Правило**: Агент не начинает реализацию сложной задачи, пока человек не утвердит план (словом "Ок", "Принято" или 👍).

## 5. Система памяти AICODE (Context Anchors)

Эта система критически важна для сохранения контекста между сессиями. Агенты имеют ограниченную память, поэтому вы **ОБЯЗАНЫ** оставлять "хлебные крошки" прямо в коде.

### Правило:

Если вы принимаете неочевидное решение, оставляете недоделку или видите потенциальную проблему — вы **обязаны** оставить соответствующий AICODE-комментарий.

Используйте формат: `# AICODE-<ТИП>: <сообщение>`

### Типы анкоров:

#### `AICODE-NOTE:` — Архитектурное решение
Объясните, почему сделано именно так.

**Пример**: `"Используем здесь polling вместо webhook, так как на dev-среде нет белого IP."`

**Когда писать**: При выборе неочевидного алгоритма, использовании "хаков" или временных решений.

#### `AICODE-TODO:` — Технический долг
Что нужно сделать, но не сейчас.

**Пример**: `"Нет валидации входящего JSON, добавить позже."`

**Когда писать**: При использовании `pass`, заглушек (stub), хардкода, отсутствии обработки edge-cases или отсутствии тестов.

**Rule of Thumb**: Если вы пишете код для MVP, задайте себе вопрос: *"Что сломается или будет неудобно, если это выкатить на 10 000 пользователей?"* Если ответ есть (нет кнопки отмены, нет лимитов на ввод, хардкод текстов) — обязательно ставьте `AICODE-TODO`.

#### `AICODE-QUESTION:` — Блокер/Вопрос
**Пример**: `"Неясно, как обрабатывать отмену подписки. Уточнить у PM."`

### Пример использования в коде:

```python
# AICODE-NOTE: Используем глобальную переменную для кэша ради MVP.
# В продакшене заменить на Redis.
user_cache = {}

async def get_user(user_id):
    # AICODE-TODO: Добавить обработку ошибок сети и rate limits
    if user_id in user_cache:
        return user_cache[user_id]
    # ...
```

## 6. Технические правила (Vibe-Coding Style)

- **Стек**: Python 3.11+, aiogram 3.x, Tortoise ORM, PostgreSQL, Anthropic Claude API.
- **Типизация**: Проект использует **строгий MyPy** — все функции должны иметь аннотации типов:
  - Используй `str | None` вместо `Optional[str]`
  - Для структурированных данных используй `TypedDict` из `src/types.py`
  - Избегай `Any` где возможно
  - Запускай `uv run mypy src/` перед коммитом
- **Линтинг**: Код проверяется **Ruff** — строгий линтер и форматтер:
  - Запускай `uv run ruff check src/` для проверки
  - Используй `uv run ruff format src/` для автоформатирования
  - Следуй PEP 8, максимум 100 символов в строке
- **Асинхронность**: Весь I/O (база данных, запросы к Telegram, запросы к Claude) должен быть асинхронным (`async`/`await`).
- **Структура**: Строго следуй структуре папок, описанной в `docs/tech.md`. Не создавай файлы в корне, если им место в `handlers/`, `services/` или `database/`.
- **Безопасность**: Токены и API ключи **только** в `.env`. Никогда не хардкодь секреты в код.
- **ORM**: Следуй правилам из пользовательских инструкций:
  - Не добавляй raw id поля для связей (используй `ForeignKeyField`).
  - Разрешено фильтровать по id (`.filter(id=...)`).
  - Миграции через Aerich.
  - Для типизации Tortoise полей используй `type: ignore[assignment]` где необходимо.

## 7. Самопроверка перед завершением

Перед тем как сказать "Готово", проверь себя:

- ✅ Код соответствует задаче?
- ✅ Если был план — выполнены ли запланированные на этот этап шаги?
- ✅ **AICODE**: Оставлены ли комментарии (`NOTE` для хаков, `TODO` для недоделок) в местах, где код несовершенен?
- ✅ Документация (`docs/`) обновлена, если что-то изменилось?
- ✅ Код выглядит опрятно (PEP8 style).
- ✅ Нет хардкода токенов и секретов?

## 8. Особенности проекта AI Sales Assistant

### Бизнес-контекст
- Проект для **малого бизнеса** — владелец бизнеса может быть **нетехническим** пользователем.
- AI-агент работает **24/7**, автономно квалифицирует лиды, ведёт диалоги и назначает встречи.
- Владелец получает **уведомления** о горячих лидах и назначенных встречах.

### Ключевые модули
- **Квалификация лидов** — через Claude API (задаёт вопросы, анализирует ответы).
- **Статусы лидов**: Холодный → Тёплый → Горячий.
- **Follow-up**: Автоматические напоминания, если лид не ответил.
- **Статистика**: Real-time для владельца.

### Tone of Voice
- Для лидов: **Дружелюбный, профессиональный, помогающий**.
- Для владельца: **Краткий, деловой, информативный**.

---

**Удачи в разработке! 🚀**

