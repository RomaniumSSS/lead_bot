# План 002: Реализация критичных функций MVP

**Дата создания**: 22.12.2025
**Статус**: В работе

---

## Objective (Цель)

Реализовать 4 критичных функции для завершения MVP:
1. Отправка материалов (портфолио/кейсы)
2. Извлечение данных из диалога (task/budget/deadline)
3. Назначение встреч с inline keyboard
4. Уведомления владельцу о встречах

---

## Context (Контекст)

База проекта готова, основной диалог работает. LLM уже возвращает action="schedule_meeting" и action="send_materials", но обработка этих action отсутствует. Нужно дореализовать эти функции для полного MVP.

**Текущее состояние:**
- ✅ Базовый диалог работает
- ✅ Claude квалифицирует лидов (HOT/WARM/COLD/NEW)
- ✅ Уведомления о горячих/тёплых лидах работают
- ❌ action="schedule_meeting" и "send_materials" не обрабатываются
- ❌ Поля task/budget/deadline не заполняются автоматически

**Ссылки:**
- `MVP_TODO.md` - детальный чеклист
- `src/handlers/conversation.py:69-74` - TODO для action
- `src/services/qualifier.py:25` - TODO для extract_lead_info
- `src/services/notifier.py:78` - TODO для notify_owner_meeting_scheduled

---

## Proposed Steps (Предлагаемые шаги)

### Этап 1: Отправка материалов (1-2 часа)

**Простая задача, сразу даст результат.**

1. **Раскомментировать поля в `src/config.py`:**
   - `portfolio_url: str | None = None`
   - `cases_url: str | None = None`
   - `presentation_url: str | None = None`

2. **Реализовать отправку материалов в `src/handlers/conversation.py:72`:**
   ```python
   elif action == "send_materials":
       materials_text = "📂 Наши материалы:\n\n"
       if settings.portfolio_url:
           materials_text += f"🌐 Портфолио: {settings.portfolio_url}\n"
       if settings.cases_url:
           materials_text += f"📋 Кейсы: {settings.cases_url}\n"
       if settings.presentation_url:
           materials_text += f"📊 Презентация: {settings.presentation_url}\n"

       materials_text += "\nЕсли возникнут вопросы — пишите!"
       await message.answer(materials_text)
   ```

3. **Обновить `.env.example`:**
   - Добавить примеры переменных (уже есть, проверить)

4. **Тестирование:**
   - Проверить, что при статусе WARM бот отправляет ссылки

**Результат:** При action="send_materials" бот отправляет ссылки на материалы.

---

### Этап 2: Извлечение данных из диалога (2-3 часа)

**Важно для заполнения полей БД и уведомлений владельцу.**

1. **Реализовать `extract_lead_info()` в `src/services/qualifier.py:25`:**
   - Загрузить историю диалога
   - Создать промпт для Claude:
     ```
     Проанализируй диалог и извлеки информацию о клиенте:
     - task: Какая задача у клиента? (одной фразой, максимум 100 символов)
     - budget: Бюджет (если упомянут, иначе null)
     - deadline: Когда нужно (если упомянут, иначе null)

     Верни ТОЛЬКО JSON: {"task": "...", "budget": "...", "deadline": "..."}
     ```
   - Отправить запрос к Claude API (использовать клиента из `llm.py`)
   - Парсить JSON ответ (учесть markdown обёртку ```json)
   - Обновить поля `lead.task`, `lead.budget`, `lead.deadline`
   - Сохранить в БД

2. **Вызывать `extract_lead_info()` в `src/handlers/conversation.py`:**
   - После обновления статуса лида (строка ~90)
   - Только если статус изменился на HOT или WARM
   - Асинхронный вызов, логировать ошибки

3. **Тестирование:**
   - Протестировать диалог, где лид упоминает задачу, бюджет, срок
   - Проверить, что поля заполняются в БД
   - Проверить, что данные видны в уведомлении владельцу

**Результат:** Поля task/budget/deadline автоматически заполняются из диалога.

---

### Этап 3: Назначение встреч (3-4 часа)

**Самая сложная задача — inline keyboard, новый handler, сохранение в БД.**

1. **Создать `src/handlers/meetings.py`:**
   - Импорты: `aiogram`, `InlineKeyboardBuilder`, `CallbackQuery`
   - Router для встреч

2. **Функция `propose_meeting_times(lead, message)`:**
   - Генерирует inline keyboard с вариантами:
     - "Сегодня 18:00" → callback_data="meeting:today_18"
     - "Завтра 10:00" → callback_data="meeting:tomorrow_10"
     - "Свое время" → callback_data="meeting:custom"
   - Отправляет сообщение: "Отлично! Когда вам удобно встретиться?"

3. **Callback handler для выбора времени:**
   - Парсить callback_data (meeting:today_18 → datetime)
   - Если "custom" → попросить ввести время текстом (упрощение для MVP)
   - Создать Meeting в БД:
     ```python
     meeting = await Meeting.create(
         lead=lead,
         scheduled_at=parsed_datetime,
         status=MeetingStatus.SCHEDULED
     )
     ```
   - Вызвать `notify_owner_meeting_scheduled(lead, meeting)`
   - Ответить лиду: "Встреча назначена! [Владелец] свяжется с вами."

4. **Интегрировать в `src/handlers/conversation.py:69`:**
   ```python
   if action == "schedule_meeting":
       from src.handlers.meetings import propose_meeting_times
       await propose_meeting_times(lead, message)
   ```

5. **Зарегистрировать router в `src/bot.py`:**
   ```python
   from src.handlers import meetings
   dp.include_router(meetings.router)
   ```

6. **Тестирование:**
   - Протестировать диалог с горячим лидом
   - Проверить inline keyboard
   - Проверить сохранение Meeting в БД
   - Проверить уведомление владельцу

**Результат:** Лид может выбрать время встречи, встреча сохраняется в БД.

---

### Этап 4: Уведомление о встречах (30 минут)

**Простая задача — форматирование и отправка.**

1. **Реализовать `notify_owner_meeting_scheduled()` в `src/services/notifier.py:78`:**
   - Формат уведомления:
     ```
     📅 **Новая встреча назначена!**

     👤 **Имя**: {lead_name}
     ⏰ **Время**: {meeting.scheduled_at.strftime("%d.%m.%Y %H:%M")}

     **Telegram**: @{username} или ID
     ```
   - Использовать тот же Bot() что и в notify_owner_about_lead
   - Обработка ошибок

2. **Тестирование:**
   - Проверить, что владелец получает уведомление после назначения встречи

**Результат:** Владелец получает уведомление о каждой назначенной встрече.

---

## Risks (Риски)

### 1. Claude API может вернуть невалидный JSON
**Митигация:**
- Уже есть обработка markdown обёртки в `llm.py`
- Добавить fallback: если парсинг failed → вернуть None для всех полей
- Логировать невалидные ответы для анализа

### 2. Inline keyboard может не работать в некоторых клиентах
**Митигация:**
- Использовать стандартный InlineKeyboardMarkup (совместимость 100%)
- Добавить fallback: "или напишите время в свободной форме"

### 3. Парсинг времени из текста ("завтра в 10") - сложная задача
**Митигация:**
- Для MVP: упростить до fixed вариантов (только сегодня 18:00 и завтра 10:00)
- Опция "свое время" → попросить ввести в формате "дд.мм.гггг чч:мм"
- Для будущего: использовать dateparser или LLM

### 4. Владелец может не получить уведомление (сеть, блокировка бота)
**Митигация:**
- Обработка ошибок в notifier.py (уже есть try/except)
- Логировать все ошибки отправки
- В будущем: добавить retry и webhook для отслеживания доставки

---

## Rollback Strategy (Стратегия отката)

Если что-то пойдёт не так:

1. **Отмена через Git:**
   - Каждый этап коммитим отдельно
   - Откат: `git revert <commit>` или `git reset --hard <commit>`

2. **Отключение функций:**
   - Закомментировать обработку action в conversation.py
   - Бот продолжит работать в базовом режиме (без встреч и материалов)

3. **База данных:**
   - Миграции не меняются (модель Meeting уже есть)
   - Откат БД не требуется

---

## Success Criteria (Критерии успеха)

### Минимальные требования (Must Have):
- ✅ При action="send_materials" бот отправляет ссылки на материалы
- ✅ Поля task/budget/deadline заполняются автоматически из диалога
- ✅ При action="schedule_meeting" лид видит inline keyboard с вариантами
- ✅ Встреча сохраняется в БД (модель Meeting)
- ✅ Владелец получает уведомление о встрече

### Желаемые требования (Nice to Have):
- ✅ Логи информативные (видно весь flow)
- ✅ Обработка ошибок не ломает диалог
- ✅ MyPy проходит без ошибок
- ✅ Ruff проходит без ошибок

### Критерии готовности к деплою:
- [ ] Полный User Flow тестирован вручную
- [ ] Проверен на реальных фразах лидов
- [ ] Владелец получает уведомления корректно
- [ ] Нет критичных TODO в коде
- [ ] Логи не содержат ошибок
- [ ] `make check` проходит успешно

---

## Estimated Time

- Этап 1: 1-2 часа
- Этап 2: 2-3 часа
- Этап 3: 3-4 часа
- Этап 4: 30 минут

**Итого: 7-10 часов чистого времени**

---

## Next Steps After MVP

После завершения этих задач:
1. Тестирование полного User Flow (как в `docs/product.md`)
2. Деплой на VPS (инструкция в `docs/deployment.md`)
3. Запуск на реальных лидах
4. Сбор обратной связи
5. Итерация (follow-up, nurture-цепочка и т.д.)

---

**План готов к утверждению.**
