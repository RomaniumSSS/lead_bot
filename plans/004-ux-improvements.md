# План: UX-улучшения диалога с лидами ✅ ВЫПОЛНЕНО

## Objective (Цель)

Исправить критические UX-проблемы в боте:
1. ✅ Отсутствует защита кнопок (можно нажимать повторно)
2. ✅ При первом /start не видно кнопок
3. ✅ Убогий прогресс-бар с избытком эмодзи
4. ✅ Время встреч только на сегодня-завтра (нереалистично)
5. ✅ Холодным лидам предлагают встречу (нелогично)
6. ✅ Сделать ответы более человечными
7. ✅ Защита от дублирования уведомлений владельцу

## Context (Контекст)

Пользователь протестировал бота и нашёл UX-проблемы:
- Кнопки можно нажимать многократно (дублируются сообщения)
- Прогресс-бар выглядит как "цирк" — избыток эмодзи
- Встречи предлагают только на "сегодня в 18:00", "завтра в 10:00" — не соответствует реальности
- Холодный лид указал "не срочно", но ему всё равно предложили встречу

## Proposed Steps (Предлагаемые шаги)

### 1. Защита кнопок от повторного нажатия
**Файлы**: `src/handlers/conversation.py`, `src/handlers/meetings.py`

- В callback handlers добавить проверку текущего state
- Если кнопка уже обработана — ответить `callback.answer("Уже обработано")` и вернуть
- Удалять клавиатуру сразу при нажатии (уже есть, но проверить)

### 2. Исправить отображение кнопок при /start
**Файлы**: `src/handlers/start.py`

- Убедиться что parse_mode="Markdown" не ломает клавиатуру
- Проверить что get_task_keyboard() возвращается корректно

### 3. Упростить прогресс-бар
**Файлы**: `src/keyboards/__init__.py`

До:
```
✅ 📋 Задача → ✅ 💰 Бюджет → ⏰ Срок ◀ → ⚪ ✅ Действие
```

После (минималистичный вариант):
```
[1] Задача → [2] Бюджет → 3. Срок → [ ] Итог
```

Или:
```
Шаг 3 из 4: Срок
```

### 4. Расширить варианты времени встреч
**Файлы**: `src/handlers/meetings.py`

- Добавить варианты: "Через 2-3 дня", "На следующей неделе"
- Генерировать конкретные даты динамически
- Улучшить формулировки (не "сегодня в 18:00", а "понедельник, 23 декабря, 18:00")

### 5. Исправить логику для холодных лидов
**Файлы**: `src/keyboards/__init__.py`, `src/handlers/conversation.py`

- Для COLD лидов **не показывать** кнопку "Назначить встречу"
- Вместо этого показывать "Получить материалы" и "Задать вопрос"
- Также убрать встречу из `get_free_chat_keyboard()` для холодных лидов

### 6. Улучшить тон сообщений (более человечно)
**Файлы**: `src/handlers/conversation.py`, `src/handlers/start.py`

- Убрать лишние эмодзи
- Сделать сообщения короче и естественнее
- Использовать более разговорный стиль

## Risks (Риски)

- ~~Изменение прогресс-бара может сломать парсинг в некоторых клиентах~~ ✅ Решено
- ~~Изменение логики квалификации может повлиять на уведомления~~ ✅ Решено

### Как решены риски с уведомлениями:

1. **Защита от дублирования**: Уведомления отправляются только при ПОВЫШЕНИИ статуса (NEW→WARM, NEW→HOT, WARM→HOT)
2. **Приоритеты статусов**: NEW=0, COLD=1, WARM=2, HOT=3 — уведомление только если new > old
3. **При /restart**: Статус НЕ сбрасывается на NEW — остаётся текущий, что предотвращает повторные уведомления

## Rollback Strategy (Стратегия отката)

- Git revert если что-то пойдёт не так
- Сохранить старую версию get_progress_indicator()

## Success Criteria (Критерии успеха)

1. ✅ Кнопки нельзя нажать повторно — бот отвечает "Уже обработано"
2. ✅ При /start кнопки видны сразу
3. ✅ Прогресс-бар минималистичный (максимум 1-2 эмодзи)
4. ✅ Варианты встреч включают "через неделю" и конкретные даты
5. ✅ Холодным лидам не предлагают встречу
6. ✅ Сообщения звучат более человечно

---

**Документ создан**: 22.12.2025
