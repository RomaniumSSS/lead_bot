# Техническая Документация

## 1. Общая Архитектура

Проект представляет собой асинхронное приложение на Python, работающее в режиме **Long Polling** (или **Webhook** в продакшене) для взаимодействия с Telegram Bot API. Приложение построено по модульному принципу с чётким разделением слоёв: интерфейс (Telegram handlers), бизнес-логика (Services), работа с данными (ORM) и внешние интеграции (LLM API).

### Принципы:

- **Асинхронность**: Все I/O операции (БД, Telegram API, Claude API) выполняются асинхронно через `async`/`await`.
- **Модульность**: Каждый модуль отвечает за свою зону ответственности (handlers, services, database, middlewares).
- **Persistence**: Вся история диалогов и данные лидов хранятся в PostgreSQL.
- **LLM-Driven**: Логика квалификации и генерация ответов управляются через Anthropic Claude API.

---

## 2. Технологический стек

### Backend:
- **Python 3.11+** — основной язык.
- **aiogram 3.x** — асинхронная библиотека для работы с Telegram Bot API.
- **Anthropic Python SDK** — клиент для Claude API.
- **Tortoise ORM 0.25.1+** — асинхронная ORM для работы с PostgreSQL.
- **asyncpg 0.30.0+** — драйвер PostgreSQL для асинхронных запросов.
- **Aerich 0.9.2+** — инструмент для миграций БД (аналог Alembic для Tortoise ORM).
- **pydantic-settings** — управление конфигурацией через `.env`.
- **python-dotenv** — загрузка переменных окружения.

### База данных:
- **PostgreSQL 15+** (в Docker контейнере для разработки, отдельный инстанс на VPS для продакшена).

### Deployment:
- **Docker + Docker Compose** — контейнеризация для удобного деплоя.
- **Ubuntu Server 22.04+** (рекомендуется для VPS).

### Инструменты:
- **uv** — быстрый менеджер зависимостей Python (альтернатива pip/poetry).
- **MyPy 1.13+** — статическая проверка типов для строгой типизации.
- **Ruff 0.8+** — быстрый линтер и форматтер кода (замена Black + Flake8 + isort).
- **pre-commit 3.5+** — автоматические проверки кода перед коммитом.
- **pytest 8.0+** — фреймворк для тестирования.
- **Git** — контроль версий.
- **Makefile** — удобные команды для разработки.

---

## 3. Структура проекта

```
ai-sales-assistant/
├── docs/                   # Документация
│   ├── product.md          # Описание продукта, User Flow
│   └── tech.md             # Техническая документация (этот файл)
├── plans/                  # Планы задач для AI-агентов
│   └── 001-initial-setup.md
├── migrations/             # Миграции БД (генерируются Aerich)
│   └── models/
├── src/                    # Исходный код приложения
│   ├── database/           # Модели Tortoise ORM
│   │   ├── __init__.py
│   │   ├── models.py       # Lead, Conversation, Meeting
│   │   └── config.py       # Конфигурация Tortoise ORM
│   ├── handlers/           # Telegram handlers (обработчики команд и сообщений)
│   │   ├── __init__.py
│   │   ├── start.py        # /start, /help команды
│   │   ├── conversation.py # Основной диалог с лидами
│   │   └── admin.py        # Команды для владельца (/stats)
│   ├── services/           # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── llm.py          # Интеграция с Claude API
│   │   ├── qualifier.py    # Квалификация лидов
│   │   ├── notifier.py     # Уведомления владельцу
│   │   └── scheduler.py    # Follow-up (на будущее)
│   ├── middlewares/        # Aiogram middlewares
│   │   ├── __init__.py
│   │   └── logging.py      # Логирование всех сообщений
│   ├── utils/              # Утилиты
│   │   ├── __init__.py
│   │   └── logger.py       # Настройка логгера
│   ├── __init__.py
│   ├── bot.py              # Инициализация бота, регистрация handlers
│   └── config.py           # Загрузка настроек из .env
├── .env.example            # Пример переменных окружения
├── .gitignore              # Игнорируемые файлы
├── .python-version         # Версия Python (для pyenv)
├── AGENTS.md               # Инструкции для AI-агентов
├── README.md               # Инструкции по запуску
├── docker-compose.yml      # Docker конфигурация
├── pyproject.toml          # Зависимости и конфигурация проекта
└── uv.lock                 # Lockfile зависимостей (генерируется uv)
```

---

## 4. Модели данных (Database Schema)

### 4.1. Lead (Лид)

Хранит информацию о потенциальном клиенте.

```python
class Lead(Model):
    id = IntField(pk=True)
    telegram_id = BigIntField(unique=True)  # Telegram User ID
    username = CharField(max_length=255, null=True)  # @username (если есть)
    first_name = CharField(max_length=255, null=True)
    last_name = CharField(max_length=255, null=True)

    # Квалификация
    status = CharEnumField(LeadStatus, default=LeadStatus.NEW)  # NEW, COLD, WARM, HOT
    task = TextField(null=True)  # Какая задача у лида
    budget = CharField(max_length=255, null=True)  # Бюджет (строка, т.к. может быть "до 50к")
    deadline = CharField(max_length=255, null=True)  # Когда нужно решить

    # Метаданные
    created_at = DatetimeField(auto_now_add=True)
    updated_at = DatetimeField(auto_now=True)
    last_message_at = DatetimeField(null=True)  # Последнее сообщение от лида

    # Связи
    conversations: ReverseRelation["Conversation"]
    meetings: ReverseRelation["Meeting"]
```

**LeadStatus** (enum):
- `NEW` — новый лид (только что написал)
- `COLD` — холодный (не квалифицирован, низкий интерес)
- `WARM` — тёплый (квалифицирован, средний интерес)
- `HOT` — горячий (готов к встрече, высокий интерес)

---

### 4.2. Conversation (Диалог)

Хранит историю всех сообщений между лидом и ботом.

```python
class Conversation(Model):
    id = IntField(pk=True)
    lead = ForeignKeyField("models.Lead", related_name="conversations", on_delete=CASCADE)

    role = CharEnumField(MessageRole)  # USER (лид) или ASSISTANT (бот)
    content = TextField()  # Текст сообщения

    created_at = DatetimeField(auto_now_add=True)
```

**MessageRole** (enum):
- `USER` — сообщение от лида
- `ASSISTANT` — сообщение от бота

**Зачем хранить историю?**
- Для передачи контекста в Claude API (лучшая квалификация).
- Для аналитики и улучшения промптов.
- Для владельца бизнеса (просмотр истории диалогов).

---

### 4.3. Meeting (Встреча)

Хранит информацию о назначенных встречах.

```python
class Meeting(Model):
    id = IntField(pk=True)
    lead = ForeignKeyField("models.Lead", related_name="meetings", on_delete=CASCADE)

    scheduled_at = DatetimeField()  # Дата и время встречи
    status = CharEnumField(MeetingStatus, default=MeetingStatus.SCHEDULED)
    notes = TextField(null=True)  # Дополнительные заметки

    created_at = DatetimeField(auto_now_add=True)
    updated_at = DatetimeField(auto_now=True)
```

**MeetingStatus** (enum):
- `SCHEDULED` — назначена
- `COMPLETED` — прошла
- `CANCELLED` — отменена

---

## 5. Основные компоненты

### 5.1. Handlers (Обработчики Telegram)

Отвечают за приём команд и сообщений от пользователей.

#### `handlers/start.py`
- Команда `/start` — приветствие нового лида, создание записи в БД.
- Команда `/help` — краткая справка.

#### `handlers/conversation.py`
- Обработка **всех текстовых сообщений** от лидов.
- Сохранение сообщения в `Conversation`.
- Вызов `llm.py` для генерации ответа.
- Отправка ответа лиду.

#### `handlers/admin.py`
- Команда `/stats` — статистика для владельца (только для `OWNER_TELEGRAM_ID`).
- Возможно, `/leads` — список всех лидов (на будущее).

---

### 5.2. Services (Бизнес-логика)

#### `services/llm.py` — Интеграция с Claude API

**Основные функции**:

```python
async def generate_response(lead: Lead, message: str) -> dict:
    """
    Генерирует ответ бота и оценивает статус лида.

    Args:
        lead: Объект лида из БД
        message: Последнее сообщение от лида

    Returns:
        {
            "response": "Текст ответа бота",
            "status": LeadStatus,  # NEW, COLD, WARM, HOT
            "action": "continue" | "schedule_meeting" | "send_materials"
        }
    """
```

**Промпт для Claude**:
- Системный промпт: описание роли бота, tone of voice, критерии квалификации.
- Контекст: история диалога (`Conversation` записи).
- Задача: сгенерировать естественный ответ и оценить статус лида.

**Пример промпта**:
```
Ты — AI-ассистент бизнеса "[Название]". Твоя задача — дружелюбно общаться с потенциальными клиентами, задавать квалифицирующие вопросы и определять их готовность к покупке.

Критерии квалификации:
- ГОРЯЧИЙ лид: чёткая задача + бюджет соответствует + срочно (на этой неделе).
- ТЁПЛЫЙ лид: задача понятна + бюджет средний + срок "скоро".
- ХОЛОДНЫЙ лид: задача неясна или бюджет низкий или "пока думаю".

История диалога:
[...]

Последнее сообщение лида: "{message}"

Ответь лиду естественным языком и оцени его статус.
```

---

#### `services/qualifier.py` — Квалификация лидов

**Функции**:
- `update_lead_status(lead, new_status)` — обновление статуса лида в БД.
- `extract_lead_info(conversation_history)` — извлечение задачи, бюджета, срока из диалога (через LLM).

---

#### `services/notifier.py` — Уведомления владельцу

**Функции**:
- `notify_owner_hot_lead(lead)` — отправляет владельцу уведомление о горячем лиде.
- `notify_owner_meeting_scheduled(lead, meeting)` — уведомление о назначенной встрече.

**Формат уведомления** (markdown):
```markdown
🔥 **Новый ГОРЯЧИЙ лид!**

👤 **Имя**: Иван Петров
📋 **Задача**: Разработка сайта для ресторана
💰 **Бюджет**: 100 000 ₽
⏰ **Срок**: На этой неделе
📅 **Встреча**: Сегодня 19:00

**Telegram**: @ivan_client
```

---

#### `services/scheduler.py` — Follow-up (на будущее, после MVP)

Планировщик автоматических напоминаний:
- Проверяет лидов, которые не отвечали 24+ часов.
- Отправляет follow-up сообщения.
- Использует `asyncio` или библиотеку типа `APScheduler`.

---

### 5.3. Middlewares

#### `middlewares/logging.py`

Логирует все входящие сообщения (для отладки и аналитики):
- Telegram User ID
- Username
- Текст сообщения
- Timestamp

---

## 6. Поток данных (Data Flow)

### Сценарий: Лид пишет сообщение

1. **Telegram API** → `handlers/conversation.py` (aiogram router)
2. `conversation.py`:
   - Получает `telegram_id`, `message.text`
   - Загружает `Lead` из БД (или создаёт, если новый)
   - Сохраняет сообщение в `Conversation` (role=USER)
3. `services/llm.py`:
   - Загружает **всю историю** диалога (`Conversation` записи)
   - Формирует промпт для Claude API
   - Отправляет запрос к Claude
   - Получает ответ + оценку статуса
4. `conversation.py`:
   - Сохраняет ответ бота в `Conversation` (role=ASSISTANT)
   - Отправляет ответ лиду через Telegram
5. `services/qualifier.py`:
   - Обновляет статус лида в БД (`Lead.status`)
6. **Если статус = HOT**:
   - `services/notifier.py` → уведомление владельцу
7. Готово ✅

---

## 7. Конфигурация (.env)

### Переменные окружения:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxx

# PostgreSQL
DATABASE_URL=postgres://user:password@localhost:5432/sales_assistant

# Owner (владелец бизнеса)
OWNER_TELEGRAM_ID=123456789

# Бизнес
BUSINESS_NAME=Моя Веб-студия
BUSINESS_DESCRIPTION=Разработка сайтов и приложений

# Режим (development / production)
MODE=development
```

---

## 8. Запуск проекта

### 8.1. Локальная разработка (без Docker)

```bash
# 1. Установить uv (если ещё нет)
curl -LsSf https://astral.sh/uv/install.sh | sh

# 2. Установить зависимости
uv sync

# 3. Запустить PostgreSQL (через Docker)
docker-compose up -d db

# 4. Создать .env (скопировать из .env.example и заполнить)
cp .env.example .env

# 5. Применить миграции
uv run aerich upgrade

# 6. Запустить бота
uv run python -m src.bot
```

---

### 8.2. Docker Compose (рекомендуется)

```bash
# 1. Создать .env
cp .env.example .env
# Заполнить .env

# 2. Запустить всё (PostgreSQL + бот)
docker-compose up -d

# 3. Применить миграции
docker-compose exec bot aerich upgrade

# 4. Проверить логи
docker-compose logs -f bot
```

---

## 9. Деплой на VPS (Ubuntu Server)

### Шаги:

1. **Подключиться к VPS по SSH**:
   ```bash
   ssh user@your-vps-ip
   ```

2. **Установить Docker и Docker Compose**:
   ```bash
   sudo apt update
   sudo apt install docker.io docker-compose-plugin -y
   sudo systemctl start docker
   sudo systemctl enable docker
   ```

3. **Склонировать репозиторий**:
   ```bash
   git clone <repository-url>
   cd ai-sales-assistant
   ```

4. **Создать .env** (с продакшн настройками):
   ```bash
   nano .env
   # Заполнить токены и DATABASE_URL
   ```

5. **Запустить через Docker Compose**:
   ```bash
   docker-compose up -d
   docker-compose exec bot aerich upgrade
   ```

6. **Настроить автозапуск** (через systemd или Docker restart policy).

---

## 10. Миграции БД (Aerich)

### Инициализация (первый раз):

```bash
aerich init -t src.database.config.TORTOISE_ORM
aerich init-db
```

### Создание миграции после изменения моделей:

```bash
aerich migrate --name "add_field_to_lead"
```

### Применение миграций:

```bash
aerich upgrade
```

---

## 11. Безопасность

### ✅ Обязательно:
- Токены и API ключи **только в `.env`** (никогда в Git).
- `.gitignore` включает `.env`, `__pycache__`, `.venv`, `*.pyc`.
- Проверка `OWNER_TELEGRAM_ID` для admin-команд (только владелец может вызывать `/stats`).

### 🔒 Рекомендации:
- Использовать webhook вместо polling на продакшене (для меньшей нагрузки).
- Настроить HTTPS для webhook (Let's Encrypt).
- Ограничить доступ к PostgreSQL (только из Docker сети или localhost).

---

## 12. Мониторинг и Логи

### Логи:
- Все сообщения логируются через `utils/logger.py`.
- Формат: `[TIMESTAMP] [LEVEL] [MODULE] Message`
- Логи сохраняются в файл `logs/bot.log` (через `RotatingFileHandler`).

### Мониторинг (на будущее):
- Sentry для отслеживания ошибок.
- Prometheus + Grafana для метрик.

---

## 13. Тестирование

### Структура тестов

Проект использует **pytest** для тестирования. Все тесты находятся в папке `tests/`.

```
tests/
├── __init__.py
├── conftest.py              # Фикстуры и настройки pytest
├── test_config.py           # Тесты конфигурации
├── test_models.py           # Тесты моделей БД
└── test_types.py            # Тесты типов данных
```

### Запуск тестов

```bash
# Через Makefile (рекомендуется)
make test              # Все тесты
make test-cov          # С покрытием кода

# Напрямую через pytest
uv run pytest tests/ -v
uv run pytest tests/ -v --cov=src --cov-report=html
```

### Типы тестов

#### Текущие тесты (MVP):
- ✅ **Unit-тесты**: конфигурация, модели БД, типы данных
- ✅ **Database-тесты**: CRUD операции, валидация, связи

#### Будущие тесты (после MVP):
- 🔄 **Service-тесты**: `services/llm.py`, `services/qualifier.py` (с моками)
- 🔄 **Integration-тесты**: полный flow (лид → диалог → квалификация → уведомление)
- 🔄 **E2E-тесты**: эмуляция Telegram пользователя через aiogram testing utils

### Инструменты

- **pytest 8.0+** — основной фреймворк
- **pytest-asyncio 0.24+** — поддержка async/await
- **pytest-cov 4.1+** — покрытие кода
- **SQLite in-memory** — быстрая тестовая БД (через conftest.py)

### Конфигурация

Настройки pytest в `pyproject.toml`:

```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
```

---

## 14. Инструменты разработки

### Makefile

Проект использует **Makefile** для удобства разработки. Все основные команды доступны через `make`:

```bash
make help              # Показать все команды
make setup             # Полная настройка окружения
make run               # Запустить бота
make check             # Проверить код (lint + type-check)
make test              # Запустить тесты
make migrate           # Применить миграции
make docker-up         # Запустить в Docker
```

**Преимущества:**
- Короткие команды вместо длинных `uv run ...`
- Единообразный интерфейс для всех операций
- Цветной вывод для удобства
- Автоматическая проверка окружения

Полный список команд: `make help` или см. `docs/development.md`.

### Pre-commit Hooks

Автоматическая проверка кода **перед каждым коммитом**.

**Установка:**
```bash
make install-hooks
```

**Что проверяется:**
- Форматирование (Ruff format)
- Линтинг (Ruff check) с автофиксом
- Проверка типов (MyPy) для src/
- Trailing whitespace
- Валидация YAML/TOML/JSON
- Debug statements (breakpoint, pdb)
- Большие файлы (>1MB)
- Merge конфликты

**Конфигурация:** `.pre-commit-config.yaml`

**Ручной запуск:**
```bash
uv run pre-commit run --all-files
```

---

## 15. Типизация и качество кода

### MyPy - Статическая проверка типов

Проект использует **строгий режим MyPy** для обеспечения безопасности типов:

```bash
# Проверка типов во всех модулях
uv run mypy src/
```

**Конфигурация** (в `pyproject.toml`):
- `strict = true` — включены все строгие проверки
- `disallow_untyped_defs = true` — все функции должны иметь аннотации типов
- `warn_return_any = true` — предупреждение при возврате Any
- Игнорируем библиотеки без типов: `aiogram`, `tortoise`, `aerich`

**Правила типизации**:
- Все публичные функции имеют полные аннотации типов
- Используем `str | None` вместо `Optional[str]` (modern Python 3.10+)
- Для сложных структур используем `TypedDict` (см. `src/types.py`)
- Для Tortoise ORM полей используем `type: ignore[assignment]` где необходимо

### Ruff - Линтинг и форматирование

**Ruff** заменяет Black, Flake8, isort и другие инструменты:

```bash
# Проверка стиля кода
uv run ruff check src/

# Автоматическое исправление
uv run ruff check --fix src/

# Форматирование кода
uv run ruff format src/
```

**Включенные правила**:
- `E`, `W` — pycodestyle (PEP 8)
- `F` — pyflakes (неиспользуемые импорты, переменные)
- `I` — isort (сортировка импортов)
- `N` — pep8-naming (именование)
- `UP` — pyupgrade (современный синтаксис)
- `ANN` — annotations (аннотации типов)
- `B` — bugbear (потенциальные баги)
- `SIM` — simplify (упрощение кода)
- `PTH` — use-pathlib (использование pathlib)
- И другие (см. `pyproject.toml`)

**Игнорируются**:
- Кириллические символы (русский проект)
- Tortoise ORM специфичные паттерны
- Некритичные правила для MVP

### src/types.py - Структурированные типы

Вместо `Dict[str, Any]` используются `TypedDict` для строгой типизации:

```python
class LLMResponse(TypedDict):
    """Структурированный ответ от LLM."""
    response: str
    status: LeadStatus
    action: Literal["continue", "schedule_meeting", "send_materials"]
```

**Преимущества**:
- MyPy проверяет наличие всех полей
- Автодополнение в IDE
- Документация структуры данных
- Меньше ошибок времени выполнения

---

## 16. Healthcheck и мониторинг

### Docker Healthcheck

Проект использует healthcheck для мониторинга работоспособности сервисов:

#### База данных (PostgreSQL)
```yaml
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U salesbot"]
  interval: 10s
  timeout: 5s
  retries: 5
```

#### Бот
```yaml
healthcheck:
  test: ["CMD", "python", "/app/healthcheck.py"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 40s
```

**Healthcheck скрипт** (`healthcheck.py`):
- Проверяет существование log файла
- Проверяет, что лог не пустой (бот пишет логи = работает)
- В продакшене можно расширить проверкой через Bot API (getMe)

**Просмотр статуса:**
```bash
docker-compose ps
```

Статусы:
- `healthy` — сервис работает нормально
- `starting` — запускается (grace period)
- `unhealthy` — проблемы после 3 неудачных попыток

### Логи

Все сообщения логируются через `utils/logger.py`:
- Формат: `[TIMESTAMP] [LEVEL] [MODULE] Message`
- Сохраняются в `logs/bot.log` через `RotatingFileHandler`
- В Docker: volume `./logs:/app/logs`

**Просмотр логов:**
```bash
# Локально
tail -f logs/bot.log

# Docker
make docker-logs
docker-compose logs -f bot
```

### Мониторинг (на будущее)
- Sentry для отслеживания ошибок
- Prometheus + Grafana для метрик
- Uptime monitoring через healthcheck endpoints

---

## 17. Технический долг (на будущее)

- [ ] Webhook вместо Long Polling (для продакшена).
- [ ] Кэширование частых запросов (Redis).
- [ ] Мультитенантность (поддержка нескольких бизнесов).
- [ ] Веб-дашборд для владельца (FastAPI + React).
- [ ] A/B тестирование промптов.
- [ ] Интеграция с Google Calendar для автоматического добавления встреч.
- [ ] Поддержка голосовых сообщений (Speech-to-Text через Whisper API).

---

**Документ актуален**: 21.12.2025
