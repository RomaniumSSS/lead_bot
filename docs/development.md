# 🛠️ Руководство по разработке

Полное руководство по инструментам, процессам и best practices для разработки AI Sales Assistant.

> **Для быстрого старта смотри:** [README.md](../README.md#-quick-start)

---

## 🔧 Makefile команды

Все основные операции доступны через `Makefile`. Чтобы увидеть полный список команд:

```bash
make help
```

### Основные команды

#### Установка и настройка
```bash
make install           # Установить зависимости
make install-dev       # Установить с dev-зависимостями
make install-hooks     # Установить pre-commit hooks
make setup             # Полная настройка (все вышеперечисленное)
```

#### Запуск
```bash
make run              # Запустить бота локально
make dev              # Запустить в dev режиме
```

#### Проверка кода
```bash
make lint             # Проверить код (Ruff)
make lint-fix         # Исправить проблемы автоматически
make format           # Отформатировать код
make type-check       # Проверить типы (MyPy)
make check            # Полная проверка (lint + type-check)
```

#### Тестирование
```bash
make test             # Запустить тесты
make test-cov         # Тесты с покрытием кода
```

#### База данных
```bash
make migrate                        # Применить миграции
make migrate-create name=описание   # Создать новую миграцию
make init-db                        # Инициализация Aerich (первый раз)
```

#### Docker
```bash
make docker-build     # Собрать образ
make docker-up        # Запустить контейнеры
make docker-down      # Остановить контейнеры
make docker-logs      # Показать логи
make docker-restart   # Перезапустить
make docker-shell     # Войти в shell контейнера
make docker-migrate   # Применить миграции в Docker
```

#### Очистка
```bash
make clean            # Очистить временные файлы
make clean-all        # Полная очистка (включая Docker)
```

---

## 🪝 Pre-commit Hooks

Pre-commit hooks автоматически проверяют код **перед каждым коммитом**.

### Установка

```bash
make install-hooks
```

Или напрямую:
```bash
uv run pre-commit install
```

### Что проверяется автоматически

- ✅ **Форматирование** — Ruff format
- ✅ **Линтинг** — Ruff check (с автофиксом)
- ✅ **Типы** — MyPy
- ✅ **Trailing whitespace** — удаление пробелов в конце строк
- ✅ **YAML/TOML/JSON** — валидация конфигурационных файлов
- ✅ **Debug statements** — проверка на breakpoint(), pdb
- ✅ **Большие файлы** — предупреждение о файлах >1MB
- ✅ **Merge конфликты** — проверка на незакрытые конфликты

### Ручной запуск

Проверить все файлы:
```bash
uv run pre-commit run --all-files
```

Проверить только изменённые:
```bash
uv run pre-commit run
```

Пропустить pre-commit (не рекомендуется):
```bash
git commit --no-verify -m "message"
```

---

## 🧪 Тестирование

### Структура тестов

```
tests/
├── __init__.py
├── conftest.py              # Фикстуры и настройки pytest
├── test_config.py           # Тесты конфигурации
├── test_models.py           # Тесты моделей БД
└── test_types.py            # Тесты типов данных
```

### Запуск тестов

```bash
# Все тесты
make test

# С покрытием кода
make test-cov

# Напрямую через pytest
uv run pytest tests/ -v
uv run pytest tests/test_models.py -v  # Конкретный файл
uv run pytest tests/test_models.py::test_create_lead -v  # Конкретный тест
```

### Написание тестов

#### Пример теста для модели:

```python
import pytest
from src.database.models import Lead, LeadStatus


@pytest.mark.asyncio
async def test_create_lead(test_telegram_id: int) -> None:
    """Тест создания лида."""
    lead = await Lead.create(
        telegram_id=test_telegram_id,
        username="test_user",
        status=LeadStatus.NEW,
    )

    assert lead.id is not None
    assert lead.status == LeadStatus.NEW
```

#### Доступные фикстуры (из `conftest.py`):

- `test_telegram_id` — тестовый Telegram ID
- `test_username` — тестовый username
- `initialize_db` — автоматическая инициализация БД (SQLite in-memory)

### Покрытие кода

После `make test-cov` откройте `htmlcov/index.html` в браузере для визуализации покрытия.

---

## 🔍 Проверка качества кода

### Ruff — Линтинг и форматирование

```bash
# Проверка кода
make lint

# Автоисправление
make lint-fix

# Форматирование
make format
```

**Настройки**: `pyproject.toml` → `[tool.ruff]`

Ruff проверяет:
- PEP 8 стиль
- Неиспользуемые импорты
- Потенциальные баги
- Именование переменных
- И многое другое

### MyPy — Проверка типов

```bash
make type-check
```

**Настройки**: `pyproject.toml` → `[tool.mypy]`

Проект использует **strict mode** — все функции должны иметь аннотации типов.

**Правила типизации:**
- Используй `str | None` вместо `Optional[str]`
- Для структур используй `TypedDict` (см. `src/types.py`)
- Избегай `Any` где возможно
- Для Tortoise ORM используй `type: ignore[assignment]` где необходимо

### Полная проверка

```bash
make check
```

Запускает `lint` + `type-check`.

---

## 🗄️ Работа с базой данных

### Создание миграции

После изменения моделей в `src/database/models.py`:

```bash
make migrate-create name="add_field_to_lead"
```

Это создаст файл миграции в `migrations/models/`.

### Применение миграций

```bash
# Локально
make migrate

# В Docker
make docker-migrate
```

### Откат миграции

```bash
make downgrade
```

### Инициализация Aerich (только первый раз)

```bash
make init-db
```

---

## 🐳 Работа с Docker

### Запуск проекта в Docker

```bash
# Запустить БД + бот
make docker-up

# Применить миграции
make docker-migrate

# Проверить логи
make docker-logs

# Проверить статус (с healthcheck)
docker-compose ps
```

### Healthcheck

Docker автоматически проверяет работоспособность:

- **БД**: `pg_isready` каждые 10 секунд
- **Бот**: `healthcheck.py` каждые 30 секунд

Статусы:
- `healthy` — всё работает
- `starting` — запускается (до 40 секунд)
- `unhealthy` — проблема (после 3 неудачных попыток)

### Вход в контейнер

```bash
make docker-shell
```

### Пересборка образа

```bash
make docker-build
make docker-up
```

---

## 📝 Workflow разработки

### Стандартный процесс

1. **Создай ветку**:
   ```bash
   git checkout -b feature/новая-фича
   ```

2. **Напиши код**:
   - Следуй PEP 8
   - Добавляй аннотации типов
   - Оставляй AICODE комментарии где нужно

3. **Проверь код**:
   ```bash
   make check
   ```

4. **Добавь тесты** (если нужно):
   ```bash
   # Создай tests/test_твой_модуль.py
   make test
   ```

5. **Коммит**:
   ```bash
   git add .
   git commit -m "feat: описание изменений"
   ```

   Pre-commit hooks автоматически проверят код.

6. **Если миграции**:
   ```bash
   make migrate-create name="описание"
   git add migrations/
   git commit -m "db: добавлена миграция"
   ```

7. **Push**:
   ```bash
   git push origin feature/новая-фича
   ```

### Быстрая проверка перед push

```bash
make check && make test
```

Если всё ОК — можно делать push.

---

## 🐛 Отладка

### Просмотр логов

```bash
# Локально
tail -f logs/bot.log

# Docker
make docker-logs
```

### Вход в Python REPL с доступом к моделям

```bash
uv run python

>>> from src.database.models import Lead
>>> from tortoise import Tortoise
>>> await Tortoise.init(db_url="...", modules={"models": ["src.database.models"]})
>>> leads = await Lead.all()
```

### Проверка переменных окружения

```bash
# В Docker
make docker-shell
env | grep TELEGRAM
```

---

## 📊 Статистика проекта

```bash
# Покрытие тестами
make test-cov

# Количество строк кода
find src/ -name "*.py" | xargs wc -l

# Количество TODO/AICODE комментариев
grep -r "AICODE-TODO" src/
```

---

## 🔗 Полезные ссылки

- **aiogram**: https://docs.aiogram.dev/
- **Tortoise ORM**: https://tortoise.github.io/
- **Anthropic Claude**: https://docs.anthropic.com/
- **Ruff**: https://docs.astral.sh/ruff/
- **MyPy**: https://mypy.readthedocs.io/
- **Pytest**: https://docs.pytest.org/

---

**Документ актуален**: 21.12.2025
